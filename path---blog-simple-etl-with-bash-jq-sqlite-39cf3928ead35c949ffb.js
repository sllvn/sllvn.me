webpackJsonp([36312840964581],{349:function(e,t){e.exports={data:{markdownRemark:{html:'<p>I run a couple simple ETL pipelines for side projects, and after experimenting with Lambda to kick off Python jobs (and finding it frustrating), I\'ve settled on simple bash scripts (along with <a href="https://github.com/stedolan/jq">the awesome <code class="language-text">jq</code> util</a>) run via cron on a DigitalOcean instance. Not only is it more stable, but I\'ve found it to be much faster. Recently, <a href="https://news.ycombinator.com/item?id=22153390">a thread on HN</a> convinced me to try adding SQLite to the mix, and I\'ve been happy with the results.</p>\n<p>For my Twitter timeline scrape, I run <a href="https://gist.github.com/sllvn/1a734d1f0c195995a9166ad309664e3f">a script</a> every five minutes that scrapes the last 200 timeline tweets (via <a href="https://github.com/twitter/twurl"><code class="language-text">twurl</code></a>), converts the JSON into a CSV, and imports that CSV into a SQLite db. A unique key on the <code class="language-text">id</code> col prevents duplicates, and so far the setup has proven solid.</p>\n<p>The trick is using <code class="language-text">jq</code>\'s <code class="language-text">@csv</code> filter (along with the <code class="language-text">-r</code> flag to not escape output as JSON) to output the tweets to CSV:</p>\n<div class="gatsby-highlight" data-language="bash">\n      <pre class="language-bash"><code class="language-bash"><span class="token keyword">echo</span> <span class="token string">\'id,created_at,user_id,screen_name,is_retweet,text\'</span> <span class="token operator">></span> tweets.csv\njq -r <span class="token string">\'.[] | [.id, .created_at, .user.id, .user.screen_name, .retweeted_status != null, .text] | @csv\'</span> data/tweets.json <span class="token operator">>></span> tweets.csv</code></pre>\n      </div>\n<p>Then all that remains is to import the CSV into your SQLite db:</p>\n<div class="gatsby-highlight" data-language="bash">\n      <pre class="language-bash"><code class="language-bash">sqlite3 tweets.sqlite -cmd <span class="token string">\'.mode csv\'</span> <span class="token string">\'.import tweets.csv tweets\'</span></code></pre>\n      </div>\n<p>You can pass a single command to <code class="language-text">sqlite3</code>\'s CLI, but when you need to chain multiple commands (as importing from CSV requires <code class="language-text">.mode csv</code>), use the <code class="language-text">-cmd</code> flag for each of the first N-1 commands (listed commands will be executed in order).</p>\n<p>The <a href="https://news.ycombinator.com/item?id=22153390">HN thread</a> gives some tips (recommended SQLite pragmas, etc.) on how to use this with larger pipelines, and I look forward to using this with my transit scraper (1000s of vehicle locations updating every minute).</p>',frontmatter:{date:"January 29, 2020",path:"/blog/simple-etl-with-bash-jq-sqlite",title:"Simple ETL with bash, jq, and SQLite"}},site:{siteMetadata:{title:"Andrew Sullivan"}}},pathContext:{}}}});
//# sourceMappingURL=path---blog-simple-etl-with-bash-jq-sqlite-39cf3928ead35c949ffb.js.map